{
  "name": "frontend-tests",
  "version": "1.0.0",
  "description": "Frontend Jest tests for Web3 ticketing dApp",
  "scripts": {
    "test": "jest --runInBand --coverage",
    "test:watch": "jest --watch",
    "test:verbose": "jest --runInBand --verbose"
  },
  "devDependencies": {
    "jest": "^30.0.0",
    "jest-environment-jsdom": "^30.0.0"
  }
}